import React from 'react';
import './DonationTiles.scss';

const DonationTiles = (props) => {
  const {
    someProp,
  } = props;
  return (
    <div>DonationTiles</div>
  );
};

export default DonationTiles;
