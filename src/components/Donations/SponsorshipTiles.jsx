import React from 'react';
import './SponsorshipTiles.scss';

const SponsorshipTiles = (props) => {
  const {
    someProp,
  } = props;
  return (
    <div>SponsorshipTiles</div>
  );
};

export default SponsorshipTiles;
