import React from 'react';
import './SoilScene.scss';

const SoilScene = (props) => {
  const {
    someProp,
  } = props;
  return (
    <section
      className="soil-scene"
    >
      ValleyScene
    </section>
  );
};

export default SoilScene;
