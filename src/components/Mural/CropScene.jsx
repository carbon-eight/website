import React from 'react';
import './CropScene.scss';

const CropScene = (props) => {
  const {
    someProp,
  } = props;
  return (
    <section
      className="crop-scene"
    >
      ValleyScene
    </section>
  );
};

export default CropScene;
